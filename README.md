
Files:
- `index.html` — entry HTML file
- `main.css` — styles
- `script.js` — small UI enhancements (theme toggle, smooth scroll)

How to view:
1. Open `index.html` in your browser (double-click or use "Open File...").
2. Or serve the folder with a static server, for example:

	python3 -m http.server 8000


Notes:
- Theme preference is saved to localStorage.
- Replace the placeholder content with your project copy and assets.